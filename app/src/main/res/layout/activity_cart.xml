<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- RecyclerView για το καλάθι -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/cartRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="gone" /> <!-- Ορίζουμε ότι είναι κρυφό αρχικά όταν το καλάθι είναι άδειο -->

    <!-- Το TextView που εμφανίζει το συνολικό ποσό -->
    <TextView
        android:id="@+id/totalAmountTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Σύνολο: €0.00"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="end"
        android:paddingTop="16dp" />

    <!-- Το κουμπί "Προσθήκη στο καλάθι" -->
    <Button
        android:id="@+id/addToCartButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Προσθήκη στο καλάθι"
        android:layout_marginTop="16dp"
        android:layout_gravity="center"
        android:padding="10dp" />

    <Button
        android:id="@+id/paymentButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Πληρωμή"
        android:layout_gravity="center"
        android:layout_marginTop="20dp"
        android:enabled="true"
        android:clickable="true" />


</LinearLayout>
